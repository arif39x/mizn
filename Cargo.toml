[workspace]
members = [
    "lib",
    "daemon",
    "tui",
]
exclude = ["bpf", "tools"]
resolver = "2"

[profile.dev]
opt-level = 3
debug = false
panic = "abort"
lto = true

[profile.release]
opt-level = 3
panic = "abort"
lto = true
codegen-units = 1
